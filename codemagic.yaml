workflows:
  ios-native-workflow:
    name: iOS Native
    max_build_duration: 120
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: Electrow
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.electro.wallet
      vars:
        BUNDLE_ID: "com.electro.wallet"
        XCODE_PROJECT: "EWallet.xcodeproj"
        XCODE_SCHEME: "EWallet"
        APP_STORE_APPLE_ID: 6785085040
      xcode: latest
    scripts:
      - name: Resolve Swift Package Dependencies
        script: |
          cd $CM_BUILD_DIR
          xcodebuild -resolvePackageDependencies -project "$XCODE_PROJECT" -scheme "$XCODE_SCHEME" -derivedDataPath DerivedData
      - name: Install Codemagic CLI tools
        script: |
          pip3 install codemagic-cli-tools
      - name: Fetch code signing
        script: |
          keychain initialize
          keychain add-certificates
          xcode-project use-profiles
      - name: Set up provisioning profiles settings on Xcode project
        script: |
          xcode-project use-profiles
      - name: Increment build number
        script: |
          cd $CM_BUILD_DIR
          LATEST_BUILD_NUMBER=$(app-store-connect get-latest-app-store-build-number "$APP_STORE_APPLE_ID")
          agvtool new-version -all $(($LATEST_BUILD_NUMBER + 1))
      - name: Build IPA
        script: |
          xcode-project build-ipa \
            --project "$XCODE_PROJECT" \
            --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.app
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.dSYM
    publishing:
      app_store_connect:
        auth: integration
        submit_to_app_store: true
